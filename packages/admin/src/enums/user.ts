export enum UserContactStatus {
  PENDING = 'pending',
  SUCCESS = 'success',
}

export enum UserFlag {
  ACTIVE = 'active',
  DELETED = 'deleted',
  SUSPENDED = 'suspended',
}

export enum MemberLevel {
  PLATINUM = 'platinum',
  GOLD = 'gold',
  SILVER = 'silver',
  HERO = 'hero',
}
